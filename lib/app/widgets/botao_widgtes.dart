import 'package:flutter/material.dart';

class BotaoWidgtes extends StatelessWidget {
  const BotaoWidgtes({Key key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
